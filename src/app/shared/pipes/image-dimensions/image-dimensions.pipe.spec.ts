import { ImageDimensionsPipe } from './image-dimensions.pipe';

describe('ImageDimensionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageDimensionsPipe();
    expect(pipe).toBeTruthy();
  });
});
